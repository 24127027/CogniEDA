# Bootstrap package

**Unsupported.** This directory has no executable bootstrap implementation.
The target bootstrap would compose application-authority services and explicit
dependencies without granting scientific authority. Directory and
documentation presence do not establish runtime assembly.

See [Current state](../../../docs/status/current-state.md).
